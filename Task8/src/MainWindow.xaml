<Window x:Class="Task8.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="400" Width="600">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Column="0" Grid.Row="0" Orientation="Vertical">
            <ComboBox Name="comboBox" SelectionChanged="ComboBox_SelectionChanged">
                <ComboBoxItem Content="Красный"/>
                <ComboBoxItem Content="Зеленый"/>
                <ComboBoxItem Content="Синий"/>
            </ComboBox>
            <Button Content="Кнопка 1" ToolTip="Это кнопка 1" Click="Button_Click"/>
            <Button Content="Кнопка 2" ToolTip="Это кнопка 2" Click="Button_Click"/>
            <Button Content="Кнопка 3" ToolTip="Это кнопка 3" Click="Button_Click"/>
            <ProgressBar Name="progressBar" Height="20" Maximum="100" Value="50"/>
        </StackPanel>

        <DockPanel Grid.Column="1" Grid.Row="0">
            <TextBox Name="textBox" Height="100" VerticalAlignment="Top"/>
            <Button Content="Добавить" DockPanel.Dock="Bottom" Click="AddButton_Click"/>
            <Slider Name="fontSizeSlider" Minimum="10" Maximum="30" ValueChanged="FontSizeSlider_ValueChanged"/>
            <DatePicker Name="datePicker" SelectedDateChanged="DatePicker_SelectedDateChanged"/>
        </DockPanel>

        <WrapPanel Grid.Column="2" Grid.Row="0">
            <CheckBox Name="checkBox" Content="Включить ProgressBar" Checked="CheckBox_Checked" Unchecked="CheckBox_Unchecked"/>
            <GroupBox Header="Выбор радиокнопок">
                <StackPanel>
                    <RadioButton GroupName="Group1" Content="Опция 1"/>
                    <RadioButton GroupName="Group1" Content="Опция 2"/>
                </StackPanel>
            </GroupBox>
            <RadioButton GroupName="Group2" Content="Опция 3"/>
            <RadioButton GroupName="Group2" Content="Опция 4"/>
            <RadioButton GroupName="Group2" Content="Опция 5"/>
        </WrapPanel>

        <Canvas Name="canvas" Grid.Column="1" Grid.RowSpan="2">
            <Ellipse Name="ellipse" Fill="Red" Width="50" Height="50"
             MouseMove="Ellipse_MouseMove" MouseLeftButtonDown="Ellipse_MouseLeftButtonDown"
             MouseLeftButtonUp="Ellipse_MouseLeftButtonUp"/>
        </Canvas>

        <ListBox Name="listBox" Grid.Column="2" Grid.Row="1" SelectionChanged="ListBox_SelectionChanged">
            <ListBoxItem Content="Элемент 1"/>
            <ListBoxItem Content="Элемент 2"/>
            <ListBoxItem Content="Элемент 3"/>
            <ListBoxItem Content="Элемент 4"/>
            <ListBoxItem Content="Элемент 5"/>
        </ListBox>

    </Grid>
</Window>
